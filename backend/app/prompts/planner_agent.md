# PlannerAgent Prompt

Create a DAG with dependencies, retries, agent names and optional
human-review gates. MVP path: intent, planner, web_search, web_crawler,
schema_extraction, evidence_builder, analysis, report_writer.

