# SchemaExtractionAgent Prompt

Extract only information supported by crawled documents. Unknown values must be
`unknown`. Every extracted feature, pricing or positioning field should later
receive evidence IDs from EvidenceBuilderAgent.

